apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: (devel)
  name: recommendedimages.paas.netease.com
spec:
  conversion:
    strategy: None
  group: paas.netease.com
  names:
    kind: RecommendedImage
    listKind: RecommendedImageList
    plural: recommendedimages
    singular: recommendedimage
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RecommendedImage is the Schema for the recommendedimages API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            properties:
              images:
                items:
                  properties:
                    module:
                      type: string
                    repository:
                      type: string
                    tags:
                      items:
                        properties:
                          deprecated:
                            type: boolean
                          description:
                            type: string
                          historyTags:
                            items:
                              type: string
                            type: array
                          name:
                            type: string
                          realTag:
                            type: string
                          targetVersion:
                            type: string
                        required:
                        - name
                        type: object
                      type: array
                  required:
                  - repository
                  - tags
                  type: object
                type: array
            required:
            - images
            type: object
        type: object
    served: true
    storage: true
